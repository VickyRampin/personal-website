image: registry.gitlab.com/paddy-hack/nikola:8.0.1

test:
  script:
  - nikola build
  except:
  - main

pages:
  script:
  - pip3 install pybtex
  - nikola plugin -i publication_list
  - nikola build
  artifacts:
    paths:
    - public
  only:
  - main
  
